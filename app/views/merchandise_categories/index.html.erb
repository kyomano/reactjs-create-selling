
<div class="wrapper">
    <% @categories.each do |category| %>
        <p>
            <%= link_to category.name, merchandise_category_path(c) %>
            <%= category.merch_count %>
        </p>
    <% end %>
</div>